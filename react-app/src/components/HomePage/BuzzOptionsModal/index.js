// import React, { useState } from "react";
// import { Modal } from "../../context/Modal";
// import BuzzOptions from "./BuzzOptions";

// function BuzzOptionsModal({ buzz }) {
//   const [showModal, setShowModal] = useState(false);

//   return (
//     <>
//       <div className="post-options-button" onClick={() => setShowModal(true)}>
//         <i className="fa-solid fa-ellipsis fa-xl"></i>
//       </div>
//       {showModal && (
//         <Modal onClose={() => setShowModal(false)}>
//           <BuzzOptions buzz={buzz} onClick={() => setShowModal(false)} />
//         </Modal>
//       )}
//     </>
//   );
// }

// export default BuzzOptionsModal;
